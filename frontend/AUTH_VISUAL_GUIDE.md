# Frontend Authentication - Visual Guide

## 🔐 Authentication Flow Diagrams

---

## 📊 Complete Login Flow

```
┌─────────────────────────────────────────────────────────────┐
│              USER OPENS APPLICATION                          │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              APP.JSX LOADS                                   │
│  <AuthProvider>                                             │
│    <Router>                                                 │
│      <Routes>                                               │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              AUTHCONTEXT INITIALIZES                         │
│  useEffect(() => checkAuth())                                │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              CHECK TOKEN IN LOCALSTORAGE                     │
│  const token = localStorage.getItem('token')                 │
└───────────────────────┬─────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Token exists │  │ No token     │  │ Invalid token│
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                  │
       ▼                 ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ GET /api/    │  │ User = null  │  │ Clear token  │
│ auth/me      │  │ isAuth = false│  │ User = null  │
└──────┬───────┘  └──────────────┘  └──────────────┘
       │
       ▼
┌──────────────┐
│ Set user    │
│ isAuth = true│
└──────────────┘
```

---

## 🔑 Login Process

```
┌──────────┐
│  User    │
│  Visits  │
│  /login  │
└────┬─────┘
     │
     │ Enters credentials
     ▼
┌─────────────────────┐
│  Login.jsx          │
│  Form submit        │
└────┬────────────────┘
     │
     │ login(formData)
     ▼
┌─────────────────────┐
│  AuthContext        │
│  login()            │
└────┬────────────────┘
     │
     │ loginApi(credentials)
     ▼
┌─────────────────────┐
│  authService.js     │
│  api.post('/auth/   │
│  login')            │
└────┬────────────────┘
     │
     │ Interceptor adds token
     ▼
┌─────────────────────┐
│  POST /api/auth/    │
│  login              │
│  { email, password }│
└────┬────────────────┘
     │
     │ Backend validates
     ▼
┌─────────────────────┐
│  Response:          │
│  {                  │
│    success: true,   │
│    token: "...",   │
│    data: { user }  │
│  }                  │
└────┬────────────────┘
     │
     │ Store in localStorage
     ▼
┌─────────────────────┐
│  localStorage:      │
│  token = "..."       │
│  user = {...}       │
└────┬────────────────┘
     │
     │ setUser(user)
     ▼
┌─────────────────────┐
│  AuthContext State: │
│  user = {...}       │
│  isAuth = true      │
└────┬────────────────┘
     │
     │ navigate('/')
     ▼
┌──────────┐
│ Dashboard│
└──────────┘
```

---

## 📝 Registration Process

```
┌──────────┐
│  User    │
│  Visits  │
│  /register│
└────┬─────┘
     │
     │ Fills form
     ▼
┌─────────────────────┐
│  Register.jsx       │
│  Form submit        │
└────┬────────────────┘
     │
     │ register(userData)
     ▼
┌─────────────────────┐
│  AuthContext        │
│  register()         │
└────┬────────────────┘
     │
     │ registerApi(userData)
     ▼
┌─────────────────────┐
│  authService.js     │
│  api.post('/auth/   │
│  register')         │
└────┬────────────────┘
     │
     │ POST /api/auth/register
     │ { name, email, password, phone }
     ▼
┌─────────────────────┐
│  Backend creates    │
│  user & returns     │
│  token              │
└────┬────────────────┘
     │
     │ Store token & user
     ▼
┌─────────────────────┐
│  localStorage:      │
│  token = "..."      │
│  user = {...}       │
└────┬────────────────┘
     │
     │ navigate('/')
     ▼
┌──────────┐
│ Dashboard│
└──────────┘
```

---

## 🛡️ Protected Route Flow

```
┌──────────┐
│  User    │
│  Navigates│
│  to /dashboard│
└────┬─────┘
     │
     ▼
┌─────────────────────┐
│  App.jsx            │
│  <Route path="/"   │
│  element={<PrivateRoute>│
│    <Dashboard />   │
│  </PrivateRoute>}   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  PrivateRoute.jsx   │
│  Checks auth        │
└────┬────────────────┘
     │
     ├──→ isAuthenticated = true
     │    ↓
     │    Render Dashboard
     │
     └──→ isAuthenticated = false
          ↓
          <Navigate to="/login" />
```

---

## 🔐 Role-Based Route Flow

```
┌──────────┐
│  User    │
│  Navigates│
│  to /complaints/create│
└────┬─────┘
     │
     ▼
┌─────────────────────┐
│  App.jsx            │
│  <RoleBasedRoute    │
│  allowedRoles={['Citizen']}>│
│    <CreateComplaint />│
│  </RoleBasedRoute>  │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│  RoleBasedRoute.jsx │
│  Checks auth & role  │
└────┬────────────────┘
     │
     ├──→ Not authenticated
     │    ↓
     │    <Navigate to="/login" />
     │
     ├──→ Authenticated but wrong role
     │    ↓
     │    <Navigate to="/" />
     │
     └──→ Authenticated & correct role
          ↓
          Render CreateComplaint
```

---

## 🔄 API Request with Token

```
┌──────────┐
│ Component│
│  Calls API│
└────┬─────┘
     │
     │ api.get('/complaints')
     ▼
┌─────────────────────┐
│  api.js             │
│  Request Interceptor│
└────┬────────────────┘
     │
     │ Gets token from localStorage
     ▼
┌─────────────────────┐
│  Adds Authorization │
│  header:            │
│  Bearer <token>     │
└────┬────────────────┘
     │
     │ HTTP Request
     ▼
┌─────────────────────┐
│  Backend            │
│  Validates token    │
└────┬────────────────┘
     │
     ├──→ Token valid
     │    ↓
     │    Process request
     │    Return data
     │
     └──→ Token invalid/expired
          ↓
          Return 401
          ↓
          Response Interceptor
          ↓
          Clear token
          ↓
          Redirect to /login
```

---

## 🚪 Logout Flow

```
┌──────────┐
│  User    │
│  Clicks  │
│  Logout  │
└────┬─────┘
     │
     │ handleLogout()
     ▼
┌─────────────────────┐
│  Navbar.jsx         │
│  logout()           │
└────┬────────────────┘
     │
     │ AuthContext.logout()
     ▼
┌─────────────────────┐
│  AuthContext        │
│  localStorage.removeItem('token')│
│  localStorage.removeItem('user')│
│  setUser(null)      │
└────┬────────────────┘
     │
     │ navigate('/login')
     ▼
┌──────────┐
│  Login   │
│  Page    │
└──────────┘
```

---

## 📊 Token Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│              TOKEN LIFECYCLE                                  │
│                                                               │
│  1. Created (Login/Register)                                │
│     ↓                                                         │
│  2. Stored in localStorage                                  │
│     ↓                                                         │
│  3. Added to API requests (Interceptor)                     │
│     ↓                                                         │
│  4. Validated by backend                                    │
│     ↓                                                         │
│  5. Used for authenticated requests                         │
│     ↓                                                         │
│  6. Expires (after 7 days)                                  │
│     ↓                                                         │
│  7. 401 Error on API call                                    │
│     ↓                                                         │
│  8. Removed from localStorage                                │
│     ↓                                                         │
│  9. User redirected to login                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: Token Storage                                     │
│  • Stored in localStorage                                    │
│  • Retrieved automatically                                  │
│  • Removed on logout/expiry                                 │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 2: Automatic Token Injection                         │
│  • Axios interceptor adds token                             │
│  • No manual token handling                                 │
│  • Works for all API calls                                  │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 3: Token Validation                                  │
│  • Validated on app load                                    │
│  • Validated on API responses                               │
│  • Auto-logout on invalid token                             │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 4: Route Protection                                  │
│  • PrivateRoute checks authentication                       │
│  • RoleBasedRoute checks role                               │
│  • Automatic redirects                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│              COMPONENT INTERACTION                            │
│                                                               │
│  App.jsx                                                     │
│    ├──→ AuthProvider                                         │
│    │     └──→ AuthContext                                    │
│    │           ├──→ Stores user state                        │
│    │           ├──→ Provides login/register/logout          │
│    │           └──→ Manages token                            │
│    │                                                          │
│    ├──→ Router                                               │
│    │     ├──→ Public Routes                                 │
│    │     │     ├──→ Login (uses AuthContext)                │
│    │     │     └──→ Register (uses AuthContext)             │
│    │     │                                                    │
│    │     └──→ Protected Routes                               │
│    │           ├──→ PrivateRoute (checks AuthContext)       │
│    │           │     └──→ Dashboard                         │
│    │           │                                              │
│    │           └──→ RoleBasedRoute (checks AuthContext)      │
│    │                 └──→ CreateComplaint (Citizen only)     │
│    │                                                          │
│    └──→ Navbar (uses AuthContext)                            │
│          ├──→ Shows user info                                │
│          └──→ Logout button                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 📱 localStorage Structure

```
localStorage
├── token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
│   └── JWT token string
│
└── user: "{\"_id\":\"...\",\"name\":\"John\",\"role\":\"Citizen\"}"
    └── User object (JSON stringified)
```

**Access:**
```javascript
// Get token
const token = localStorage.getItem('token')

// Get user
const user = JSON.parse(localStorage.getItem('user'))

// Set token
localStorage.setItem('token', token)

// Remove token
localStorage.removeItem('token')
```

---

## 🔄 State Flow

```
┌─────────────────────────────────────────────────────────────┐
│              STATE FLOW                                       │
│                                                               │
│  AuthContext State:                                          │
│  {                                                            │
│    user: null | { _id, name, email, role },                 │
│    loading: true | false,                                   │
│    isAuthenticated: false | true                             │
│  }                                                            │
│                                                               │
│  State Changes:                                              │
│  1. Initial: user = null, isAuth = false                     │
│  2. Login: user = {...}, isAuth = true                      │
│  3. Logout: user = null, isAuth = false                     │
│  4. Token Expiry: user = null, isAuth = false               │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ Summary

**Authentication Flow:**
1. App loads → Check token → Set user state
2. User logs in → Store token → Set user state → Redirect
3. Protected route → Check auth → Render or redirect
4. API request → Add token → Validate → Return data
5. Token expires → Clear token → Redirect to login

**Key Components:**
- AuthContext - State management
- Login/Register - Authentication forms
- PrivateRoute - Route protection
- RoleBasedRoute - Role-based protection
- api.js - Token injection

**Security:**
- Token in localStorage
- Automatic token injection
- Token validation
- Auto-logout on expiry
- Protected routes

This visual guide shows the complete frontend authentication flow! 🔐
